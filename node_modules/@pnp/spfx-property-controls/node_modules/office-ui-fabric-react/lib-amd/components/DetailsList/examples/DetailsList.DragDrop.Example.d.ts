/// <reference types="react" />
import * as React from 'react';
import { IColumn } from 'office-ui-fabric-react/lib/DetailsList';
import './DetailsList.DragDrop.Example.scss';
export declare class DetailsListDragDropExample extends React.Component<{}, {
    items: {}[];
    selectionDetails?: string;
    columns: IColumn[];
    isColumnReorderEnabled: boolean;
    frozenColumnCountFromStart: string;
    frozenColumnCountFromEnd: string;
}> {
    private _selection;
    constructor(props: {});
    render(): JSX.Element;
    private _handleColumnReorder(draggedIndex, targetIndex);
    private _getColumnReorderOptions();
    private _validateNumber(value);
    private _onChangeStartCountText(text);
    private _onChangeEndCountText(text);
    private _onChangeColumnReorderEnabled(checked);
    private _getDragDropEvents();
    private _onItemInvoked(item);
    private _onRenderItemColumn(item, index, column);
    private _insertBeforeItem(item);
}
