/// <reference types="react" />
import { BaseComponent } from '../../Utilities';
import { ITooltipHostProps } from './TooltipHost.types';
import { TooltipDelay } from './Tooltip.types';
export interface ITooltipHostState {
    isTooltipVisible: boolean;
}
export declare class TooltipHost extends BaseComponent<ITooltipHostProps, ITooltipHostState> {
    static defaultProps: {
        delay: TooltipDelay;
    };
    private _tooltipHost;
    private _closingTimer;
    constructor(props: ITooltipHostProps);
    render(): JSX.Element;
    private _getTargetElement();
    private _onTooltipMouseEnter;
    private _onTooltipMouseLeave;
    private _clearDismissTimer;
    private _hideTooltip;
    private _toggleTooltip(isTooltipVisible);
}
