import { IStyle } from '@uifabric/merge-styles/lib/index';
import { IClassNames } from './IClassNames';
import { IStyleFunction } from './IStyleFunction';
/**
 * Creates a getClassNames function which calls getStyles given the props, and injects them
 * into mergeStyleSets.
 */
export declare function classNamesFunction<TStyleProps extends {}, TStyles extends {
    [P in keyof TStyles]: IStyle;
}>(): (getStyles?: IStyleFunction<TStyleProps, TStyles>, styleProps?: TStyleProps) => IClassNames<TStyles>;
