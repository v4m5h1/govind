/// <reference types="react" />
import * as React from 'react';
import { IComponentStatusProps } from './ComponentStatus.types';
import './ComponentStatus.scss';
export declare class ComponentStatus extends React.Component<IComponentStatusProps, {}> {
    static defaultProps: IComponentStatusProps;
    render(): JSX.Element;
    private _definebadgeAnchor(subject, state);
    private _badgeAnchor(ariaLabel, color, subject, status);
    private _badgeURL(color, subject, status);
    private _colorForComponentStateStatus(testCoverageStatus);
}
