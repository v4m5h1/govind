import { Validate } from '@microsoft/sp-core-library';
var SPListItem =  (function () {
    function SPListItem(data) {
        this._validate(data);
        this._id = data.id;
    }
    SPListItem.prototype._serialize = function () {
        return {
            id: this.id
        };
    };
    Object.defineProperty(SPListItem.prototype, "id", {
        get: function () {
            return this._id;
        },
        enumerable: true,
        configurable: true
    });
    SPListItem.prototype._validate = function (data) {
        Validate.isNotNullOrUndefined(data, 'ISPListItemData object');
        Validate.isNotNullOrUndefined(data.id, 'id');
    };
    return SPListItem;
}());
export default SPListItem;
