import { IPackageDeps } from './IPackageDeps';
/**
 * Parses the output of the "git ls-tree" command
 */
export declare function parseGitLsTree(output: string): Map<string, string>;
/**
 * Parses the output of the "git status" command
 */
export declare function parseGitStatus(output: string, packagePath: string): Map<string, string>;
/**
 * Takes a list of files and returns the current git hashes for them
 */
export declare function gitHashFiles(filesToHash: string[], packagePath: string): Map<string, string>;
/**
 * Executes "git ls-tree" in a folder
 */
export declare function gitLsTree(path: string): string;
/**
 * Executes "git status" in a folder
 */
export declare function gitStatus(path: string): string;
/**
 * Collects the current git filehashes for a directory
 * @public
 */
export declare function getPackageDeps(packagePath?: string, excludedPaths?: string[]): IPackageDeps;
