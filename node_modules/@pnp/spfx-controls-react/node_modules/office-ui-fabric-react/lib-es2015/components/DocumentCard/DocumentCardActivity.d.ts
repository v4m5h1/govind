/// <reference types="react" />
import { BaseComponent } from '../../Utilities';
import { IDocumentCardActivityProps } from './DocumentCard.types';
export declare class DocumentCardActivity extends BaseComponent<IDocumentCardActivityProps, any> {
    render(): JSX.Element | null;
    private _renderAvatars(people);
    private _renderAvatar(person);
    private _getNameString(people);
}
