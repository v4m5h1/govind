/// <reference types="react" />
import * as React from 'react';
import './ComponentStatusPage.scss';
export declare class ComponentStatusPage extends React.Component<{}, {}> {
    render(): JSX.Element;
    private _renderOverView();
    private _renderComponents();
    private _renderComponent(componentName);
    private _renderStatusesInfo();
    private _renderStatusInfo(statusInfo);
    private _createLink(information);
}
