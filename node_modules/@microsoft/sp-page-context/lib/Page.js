import { Validate } from '@microsoft/sp-core-library';
var Page =  (function () {
    function Page(data) {
        this._validate(data);
        this._socialBarEnabled = data.socialBarEnabled || false;
    }
    Object.defineProperty(Page.prototype, "socialBarEnabled", {
        get: function () {
            return this._socialBarEnabled;
        },
        enumerable: true,
        configurable: true
    });
    Page.prototype._validate = function (data) {
        Validate.isNotNullOrUndefined(data, 'IPage object');
    };
    return Page;
}());
export default Page;
